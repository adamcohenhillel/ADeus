import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ai.adeus',
  appName: 'adeus',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
